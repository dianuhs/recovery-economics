# Artifacts

This is where Recovery Economics writes outputs:

- artifacts/reports/ for human-readable markdown summaries
- artifacts/data/ for machine-readable JSON results

You can commit example reports here, but in real use this folder is usually gitignored
and generated at runtime.
